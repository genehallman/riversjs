const Messages = require('./messages');
const Users = require('rivers/server/api/users');
const Permissions = require('rivers/server/api/permissions');

exports = module.exports = {
  Messages,
  Users,
  Permissions
};